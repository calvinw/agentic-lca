name: Update sessions.json

on:
  push:
    paths:
      - 'skills_sessions/*.md'

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Regenerate sessions.json
        run: python3 skills_sessions/generate_sessions.py

      - name: Commit updated sessions.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add skills_sessions/sessions.json
          git diff --cached --quiet || git commit -m "chore: regenerate sessions.json"
          git push
